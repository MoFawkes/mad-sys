<ui:FluentWindow x:Class="AqiClock.App.Views.RoleChoiceWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
  Title="Choose how to continue"
  Width="860" Height="620" MinWidth="680" MinHeight="440"
  WindowStartupLocation="CenterScreen" ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica">
  <ui:FluentWindow.Resources>
    <Style x:Key="AudienceCardStyle" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource CardBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="28" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Card"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="12"
                    Padding="{TemplateBinding Padding}"
                    RenderTransformOrigin="0.5,0.5">
              <Border.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1" /></Border.RenderTransform>
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Card" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                <Setter TargetName="Card" Property="Effect">
                  <Setter.Value><DropShadowEffect BlurRadius="18" ShadowDepth="5" Opacity="0.22" /></Setter.Value>
                </Setter>
                <Setter TargetName="Card" Property="RenderTransform">
                  <Setter.Value><ScaleTransform ScaleX="1.015" ScaleY="1.015" /></Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                <Setter TargetName="Card" Property="BorderThickness" Value="3" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Card" Property="RenderTransform">
                  <Setter.Value><ScaleTransform ScaleX="0.985" ScaleY="0.985" /></Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Card" Property="Opacity" Value="0.5" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="ActionLabelStyle" TargetType="StackPanel">
      <Setter Property="Opacity" Value="0.45" />
      <Style.Triggers>
        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True"><Setter Property="Opacity" Value="1" /></DataTrigger>
        <DataTrigger Binding="{Binding IsKeyboardFocused, RelativeSource={RelativeSource AncestorType=Button}}" Value="True"><Setter Property="Opacity" Value="1" /></DataTrigger>
      </Style.Triggers>
    </Style>
  </ui:FluentWindow.Resources>

  <Grid Background="{DynamicResource WindowBrush}">
    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
    <ui:TitleBar Title="AQI Clock" />
    <Grid Grid.Row="1" Margin="44,18,44,38">
      <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
      <StackPanel HorizontalAlignment="Center" Margin="0,0,0,30">
        <TextBlock Text="AQI Clock" FontSize="38" FontWeight="Bold" Foreground="{DynamicResource HeaderBrush}" HorizontalAlignment="Center" />
        <TextBlock Text="Choose the audience using this computer." FontSize="18" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Center" Margin="0,7,0,0" />
      </StackPanel>

      <Grid Grid.Row="1" MaxWidth="760" MinHeight="210">
        <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="24" /><ColumnDefinition /></Grid.ColumnDefinitions>
        <Button x:Name="TeacherOption" Grid.Column="0" MinHeight="210" Style="{StaticResource AudienceCardStyle}"
                Click="OnTeacher" TabIndex="0" automation:AutomationProperties.Name="Continue as teacher">
          <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
          <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="82" Height="82" CornerRadius="41" Background="{DynamicResource ControlFillColorSecondaryBrush}" HorizontalAlignment="Center">
              <ui:SymbolIcon Symbol="HatGraduation24" FontSize="40" Foreground="{DynamicResource AccentBrush}" />
            </Border>
            <TextBlock Text="Teacher" FontSize="26" FontWeight="SemiBold" Foreground="{DynamicResource HeaderBrush}" HorizontalAlignment="Center" Margin="0,22,0,0" />
            <StackPanel Style="{StaticResource ActionLabelStyle}" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
              <TextBlock Text="SIGN IN" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" />
              <ui:SymbolIcon Symbol="ArrowRight20" Foreground="{DynamicResource AccentBrush}" Margin="7,0,0,0" />
            </StackPanel>
          </StackPanel>
          </Viewbox>
        </Button>

        <Button x:Name="StudentOption" Grid.Column="2" MinHeight="210" Style="{StaticResource AudienceCardStyle}"
                Click="OnStudent" TabIndex="1" automation:AutomationProperties.Name="Continue as student">
          <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
          <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Width="82" Height="82" CornerRadius="41" Background="{DynamicResource ControlFillColorSecondaryBrush}" HorizontalAlignment="Center">
              <ui:SymbolIcon Symbol="Person24" FontSize="40" Foreground="{DynamicResource AccentBrush}" />
            </Border>
            <TextBlock Text="Student" FontSize="26" FontWeight="SemiBold" Foreground="{DynamicResource HeaderBrush}" HorizontalAlignment="Center" Margin="0,22,0,0" />
            <StackPanel Style="{StaticResource ActionLabelStyle}" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
              <TextBlock Text="CHOOSE CLASSES" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" />
              <ui:SymbolIcon Symbol="ArrowRight20" Foreground="{DynamicResource AccentBrush}" Margin="7,0,0,0" />
            </StackPanel>
          </StackPanel>
          </Viewbox>
        </Button>
      </Grid>
    </Grid>
  </Grid>
</ui:FluentWindow>










